This is a simple web service to expose the database.

Getting started:
  Install the latest version of nodejs.
  Use npm to install the following packages (from nodejs directory, 'npm install <package>'):
    - express
    - mssql
    - fs
    - pug
   Fill in the database and install path details in server.js (line 10-13).
   Run 'node.exe server.js'